# {{ title }}

{% if purpose %}
**Purpose.** {{ purpose }}
{% endif %}
{% if in_scope %}
**In scope:** {{ in_scope | join(", ") }}
{% endif %}
{% if out_of_scope %}
**Out of scope:** {{ out_of_scope | join(", ") }}
{% endif %}

## Relationship map

!!! tip "Interactive Diagram"
    **Click on the diagram below to zoom in.** Click again to zoom out.
    
    The diagram shows the relationships between the key sections in this vertical domain.

{{ mermaid_block }}

## Key sections

{# Use a real Markdown table to avoid whitespace collapsing #}
| Section | Description | MetaInfo |
|---|---|---|
{% for s in section_info %}
| `{{ s.name }}` | {{ s.description }} | [Open in MetaInfo browser]({{ s.metainfo_url }}){:target="_blank"} |
{% endfor %}


## Micro-examples

=== "YAML"

    ```yaml
    {{ example_yaml | trim | indent(4) }}
    ```
